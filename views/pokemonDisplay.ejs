<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= pokemon.name.charAt(0).toUpperCase() + pokemon.name.slice(1) %> - Pokédex</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/styles.css">
</head>
<body class="text-center">

  <h1 class="text-white p-5" style="background-color: #EE1B24; font-size: 5rem">Pokédex</h1>
  <%- include('partials/navbar') %>

  <div class="container mt-5">
    <h1 class="mb-4 text-capitalize"><%= pokemon.name %> (ID: <%= pokemon.id %>)</h1>


    <img src="<%= pokemon.sprites.front_default %>" alt="<%= pokemon.name %>" class="mb-4" style="width:150px; height:auto;">

  <table class="table table-bordered w-75 mx-auto">
  <tbody>

    <!-- Height -->
    <tr>
      <th scope="row">Height</th>
      <td><%= pokemon.height %> decimetres</td>
    </tr>

    <!-- Weight -->
    <tr>
      <th scope="row">Weight</th>
      <td><%= pokemon.weight %> hectograms</td>
    </tr>

    <!-- Types-->
    <tr>
      <th scope="row">Types</th>
      <td>
        <% 
        // Loop through each type
        for (let i = 0; i < pokemon.types.length; i++) { 
          let typeName = pokemon.types[i].type.name; 
        %>
          <span class="badge bg-primary me-1"><%= typeName %></span>
        <% } %>
      </td>
    </tr>

    <!-- Abilities-->
    <tr>
      <th scope="row">Abilities</th>
      <td>
        <% 
        // Loop through each ability
        for (let i = 0; i < pokemon.abilities.length; i++) { 
          let abilityName = pokemon.abilities[i].ability.name; 
        %>
          <span class="badge bg-success me-1"><%= abilityName %></span>
        <% } %>
      </td>
    </tr>

    <!-- Stats -->
    <tr>
      <th scope="row">Base Stats</th>
      <td>
        <% 
        // Loop through each stat
        for (let i = 0; i < pokemon.stats.length; i++) { 
          let statName = pokemon.stats[i].stat.name;
          let statValue = pokemon.stats[i].base_stat;
        %>
          <strong><%= statName %>:</strong> <%= statValue %> <br>
        <% } %>
      </td>
    </tr>

  </tbody>
</table>

    <a href="/" class="btn btn-secondary mt-4">Back to Search</a>
  </div>

  <%- include('partials/footer') %>

</body>
</html>
